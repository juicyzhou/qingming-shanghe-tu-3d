# 《汴京漫游》迭代记录

> 共 21 个提交，覆盖：初始构建 → 玩法完善 → 性能优化 → 移动端适配 → 大规模 Bug 修复 → 发布打磨
> 最新提交：`cd97ec3`（移动端清晰度）；之后进入第一批优化（P0-1/2/3 + P1-1）

## 阶段一：初始构建与部署

| 提交 | 内容 |
|---|---|
| `379790f` | **3D《清明上河图》沉浸式漫游游戏**：完整首版（虹桥/汴河/10 店/46 NPC/8 任务/对话/经济/结算/程序化音效/双视角），零外部资源，可部署 Pages |
| `6300c94` | **部署 Actions 升级**：checkout@v7 / setup-node@v7 / upload-pages-artifact@v5 / deploy-pages@v5，消除 Node 20 弃用警告 |

## 阶段二：进店与基础体验修复

| 提交 | 内容 |
|---|---|
| `a0b2182` | **进屋三项问题**：①第一人称前后方向反（相机朝向+π）②屋内灰色平面（室内材质加暖色环境光增益 uBoost）③门看起来关着（门洞/门扇朝向修正为平行墙面+双面渲染） |

## 阶段三：系统自测与数据修正

| 提交 | 内容 |
|---|---|
| `a42bbe4` | **体验测试修复**：任务 NPC 坐标错位/重叠（茶博士进店、赵大夫/沈掌柜跑错街）、凉粉摊建在汴河里、码头长树挡住船老大、码头物品陷入桥面、进店视角模式记忆 |

## 阶段四：性能优化 + 手机端适配

| 提交 | 内容 |
|---|---|
| `2c14545` | **性能优化**：建筑按材质合并几何（~40→8 draw call）、树木 InstancedMesh（360→4）、摊位/船/灯笼合并、角色臂+手/腿+脚合并、远处 NPC 距离剔除、像素比上限 1.5。**手机端适配**：虚拟摇杆+右侧视角+交谈/视角/任务按钮、撑船触屏推桨、触屏自动降档（像素比1/关描边）、HUD 响应式。实测可见 draw call 2588→812 |

## 阶段五：移动端方向与按钮

| 提交 | 内容 |
|---|---|
| `35e3ab6` | **手机端左右反向与交谈按钮常显**：视角符号修正、摇杆/视角按 touch id 追踪（修多指误触）、交谈按钮按需显示 |

## 阶段六：方向约定审计

| 提交 | 内容 |
|---|---|
| `1429c11` | **全面审计移动/旋转**：交叉验证 forward/right/heading/相机一致性，新增四方向测试（W远离/S靠近/D右/A左）+ 第一人称 W 沿视线测试，Player 顶部补方向约定注释 |

## 阶段七：建筑外观打磨

| 提交 | 内容 |
|---|---|
| `2bb9746` | **建筑外观修复**：消除反光/曝白（光照降到受光面≈1.0，实测 glare=0%）、布幌横杆旋转参数修正、门洞暗面移到墙面外、门框改柱+楣、雨搭改朝外斜篷、说书棚招牌朝观众、室内门洞与碰撞对齐、二层栏杆缩短、rim 降强度 |

## 阶段八：室内问题修复

| 提交 | 内容 |
|---|---|
| `3920cf9` | **掌柜与家具交叉 + 半空灰色平面**：interiors 为每店预留 keeperPos 空位、掌柜按站位摆放面向店门（QA keeperFree 验证）；toon 暗部明度保底 0.12→0.22 消除背阴灰面 |
| `6bf0c93` | **灰蓝平面（透明材质根因）**：toon 恒输出 alpha=1 导致透明材质（幌子/光晕/帆/卷轴）不透明+深度排序闪烁；正确支持 opacity 与贴图 alpha；客栈屏风改双面暖灰绿；标签底不透明度提高 |

## 阶段九：README 与宣传截图

| 提交 | 内容 |
|---|---|
| `853a995` | README 增加在线体验链接与游戏截图 |
| `c7ab3b9` | 修复 README 截图：改为后处理真实游戏帧，四视角各不相同（此前 headless 抓帧过早全是同一视角） |
| `08cac93` | 重截主街截图：斜向构图、两侧店铺+街上行人、去玩家遮挡 |
| `01fea10` | 调试钩子：street3 视角、探针导出后处理帧 |

## 阶段十：神秘"灰蓝平面"攻坚

> 用户持续报告进店后"凭空出现的无材质平面、半空、弹动、出现消失"，多轮定位：

| 提交 | 内容 |
|---|---|
| `a8eb06e` | **灰蓝平面多层防御**：室内墙改单面朝内、布幌/雨搭/船帆改不透明、屏风/卷轴暖色、室内增益 0.5→0.9 |
| `0fa5ded` | **nocomposer 诊断开关**（绕过后处理二分定位）；NaN/退化/坏挂载扫描 |
| `d1f2a1e` | **恢复画面色彩**（此前光照降太狠导致颜色坏掉）+ toon NaN 防护（normalize(0) 兜底）+ 柔和高光压缩 1-exp(-col*1.5) |
| `f8a6157` | **调试射线检测**（?debug=1 按钮 / 桌面 F 键），让用户在真机定位命中物体 |
| `437d962` | **🎯 根因定位**：用户用调试射线命中 `hex=3a5a8a`（玩家自己的蓝色书生长袍）。第一人称低头看到自己身体挡视线，误判为"凭空平面"。**修复**：第一人称隐藏自身身体（`visible = viewMode===3`） |

## 阶段十一：移动端清晰度

| 提交 | 内容 |
|---|---|
| `cd97ec3` | **移动端清晰度**：触屏像素比上限 1→2（原 DPR 2~3 手机只用了 1/3 分辨率 → 严重模糊），桌面保持 1.5 |

## 阶段十二：第一批优化（P0 核心 + P1 分享点）

| 提交 | 内容 |
|---|---|
| `605eb12` | **P0-1 任务指引**：`Game.getGuideTarget()` 取第一个进行中任务的目标（NPC/场景交互物），无任务时指向首个可接任务人；HUD 屏幕边缘方向箭头（目标在背后自动镜像）+ 距离标签「☛ 任务名 · 38m」+ 小地图金色目标点。**P0-2 开场引导**：进京赶考书生剧情 2 屏 + 操作一屏一讲（桌面/触屏两版）+ 引导结束自动锁定指针；`localStorage` 记忆已看过，`autostart=1` 跳过。**P0-3 暂停/设置菜单**：Esc/触屏「菜单」按钮 → 继续/音量滑块（`AudioSys.setVolume`，localStorage 记忆）/重新开始/返回标题；小玩法中 Esc 直接取消小玩法。**P1-1 成就分享卡**：全任务完成弹「画卷集齐」卡片（Canvas 生成：实拍帧 + 印章 + 4 项统计，PNG 可下载/长按分享） |

## 阶段十三：第二批优化（P0 补全 + 沉浸/移动端/音效）

| 提交 | 内容 |
|---|---|
| `48a8ea0` | **P0-4 WebGL 兼容**：启动前 `webglAvailable()` 检测 + `try/catch` 兜底 → 羊皮纸友好提示页（更新浏览器/硬件加速/换设备/重试），无 WebGL 不白屏。**P0-5 加载进度**：index.html 内联水墨风加载动画（`__loadProgress` 推进：铺卷→装裱→唤起汴京→完成淡出）。**P1-2 沉浸氛围**：环境音三层（汴河低噪+市井人声+随机鸟鸣，`startAmbient` 合成）；脚步随地面变化（`layout.groundType`：石板/木桥/草地，接入 Player.onStep，此前进脚步从未触发）；傍晚暖光（`LIGHT_UNIFORMS` 太阳略低更暖 + 雾更沉，`?day=1` 恢复正午 A/B 验证：墙体更暖、glare/gray 均为 0）。**P1-3 移动端**：竖屏 FOV 62→78；触屏按钮 safe-area 防刘海/手势条遮挡；自适应像素比（帧耗时 >36ms 降档、<20ms 升回，上限触屏 2/桌面 1.5，headless 实测正确降档）。**P1-4 音效细化**：开门吱呀/对话翻页/结算盖章/购买成交，接进店、对话、结算、购买全链路；AudioSys 增加 `isFinite(currentTime)` 防御（无音频设备环境不崩溃） |

## 阶段十四：第三批优化（P1 收尾 + 发布传播）

| 提交 | 内容 |
|---|---|
| `7dbf22e` | **P1-5 桌面描边可选**：composer 暴露 `outlinePass`，暂停菜单加「墨线描边」开关（运行时 enabled 切换 + localStorage 记忆，触屏恒关）。**P3-1 分享元信息**：index.html 增加 og/twitter meta（title/description/type/url/image）+ 内联 SVG 汴字 favicon；生成 `public/og-cover.jpg`（1200×630，headless 截取空中全景，随构建发布，微信/QQ 分享显示缩略图）。**P3-2 宣传素材**：小红书竖版海报（1080×1440）/ 公众号横版海报（1920×1080，HTML 模板叠字后截图）/ 知乎技术文（promo/tech-article.md，约 1800 字，含着色器代码与坑）；短视频因环境无 ffmpeg 交付 `capture-cinematic.mjs` 帧序列脚本 + 编码命令。**P3-3 微信兼容**：`window.__inWeChat` UA 检测 + `enableOnGesture` 音频手势兜底（WKWebView 常见）；真机 12 项验收清单 docs/06。另加 `?cinematic=1` 自由相机持续运行（宣传帧捕获用） |

## 阶段十五：P2 内容扩展（打卡收集 + NPC 作息 + 任务扩展）

| 提交 | 内容 |
|---|---|
| `185fb4e` | **P2-3 打卡收集**：12 处景点自动打卡（虹桥/码头/城门/醉仙楼/清风茶肆/悦来客栈/回春堂/锦绣布庄/说书棚/糖人摊/桥南茶摊/丰源米铺，`data/landmarks.js`）；画卷图鉴面板（J 键，横向画卷 + 12 枚印章 + 景点列表）；集齐弹「画卷图鉴」成就卡（`showAchievement` 双模式）。**P2-2 NPC 作息**：时辰时钟（HUD 顶部子丑寅卯 + 白日/夜色，默认未时开跑，一日 8 分钟，`?hour`/`?daylen`/`?nocycle` 控制）；昼夜光照（白昼 = 原暖色画风零回归，入夜整体微暗 + 夜空转深，灯笼暖调保可见，probe 实测 glare/gray 为 0）；摊贩白天出摊入夜收摊抽稀、更夫/巡夜 2 名夜巡人入夜上街；任务 NPC 恒显不破任务。**P2-4 任务扩展**：8 → 13 条（客栈添柴/醉仙楼送酒/米铺送粮/卦摊猜谜[新 riddle 目标类型，选对推进选错提示]/说书人新书[5 步长线]）；新增 4 个场景物品（木柴/酒坛/米袋/手稿）+ 卦师 NPC（许半仙）。selftest 扩至 13 条全通 + 打卡 + 昼夜，features/qa 全过 |

## 阶段十六：P2-1 更多小玩法（听书点唱 + 夜市灯谜 + 撑船竞速）

| 提交 | 内容 |
|---|---|
| `219b6d7` | **听书点唱**：说书棚新增「听书」场景物（醒木台），点选 3 段故事（武松打虎/孟姜女/白蛇传）逐句讲，结尾可鼓掌（+5 声望）或打赏 10 文（+10 声望）。**夜市猜灯谜**：集市口新增花灯场景物，入夜才开张（白天提示"入夜可来猜谜"）；6 题谜池，答对 +25 文/+5 声望并记入 `lanternRiddles`，全解再奖 30 文；答错灯笼摇晃不推进。**撑船竞速**：码头新增竞速舟，连按空格/触屏「推桨」推舟 vs 自动前进的对手舟，先到 10 胜（+40 文/+5 声望）。三面板均 DOM 实现（`data/minigames.js` 数据），Game 提供可单测的结算方法（`answerLanternRiddle`/`finishRace`/`storyReward`）；E 键可关闭已开面板、竞速中冻结玩家。selftest 扩至 13 任务+打卡+昼夜+三小玩法全过 |

## 阶段十七：P2-5 天气系统（雨雪 + 水面光斑）

| 提交 | 内容 |
|---|---|
| `dddeb0a` | **雨雪粒子**：`render/weather.js` 新增天气系统——雨 500 条线雨丝（LineSegments）+ 雪 420 个柔白点（Points），粒子跟随玩家、按时间下落/摇摆、顶部循环；40~90s 随机换天（晴 55% / 雨 30% / 雪 15%，夜晚更易下雪），约 2.5s 平滑过渡。**水面光斑**：水面 shader 新增 `uSparkle`（太阳细碎光斑，随昼夜/晴雨强弱）与 `uRain`（雨天水色变暗、波纹加密）；雨天整体光照再暗 14%；雨声层（`AudioSys.setRain` 中高频噪声淡入淡出）。调试参数 `?weather=rain/snow/clear` 强制指定。selftest 扩至 13 任务+打卡+昼夜+三玩法+天气全过（weather=ok sparkle=ok），features/qa 全过 |

## 阶段十八：P3-4 体验埋点（本地轻量漏斗）

| 提交 | 内容 |
|---|---|
| `39c2ea0` | **埋点**：`game/Analytics.js` 本地统计（localStorage，零外部请求，保持"离线可玩"）——会话数 / 累计与平均停留（每帧心跳 + beforeunload 落盘）/ 首任务接受与完成（每会话各计一次）/ 任务完成 / 进店 / 小玩法获胜 / 灯谜 / 图鉴集齐。**面板**：`?analytics=1` 打开羊皮纸数据面板（11 行指标 + 导出 JSON + 清空），`window.__analytics()` 供控制台/巡检。修复一处真实回归：`TEST_PARAMS` 原声明在 Game 构建之后 → TDZ ReferenceError 会在每次正常加载时误触发 WebGL 兼容页，已上移到文件顶部。selftest 扩至 13 任务+打卡+昼夜+三玩法+天气+埋点全过，features/qa 全过 |

## 阶段十九：电脑/手机 E2E 实测 + 关键 Bug 修复

> 新增可复用 E2E 套件 `tests/e2e/`（driver.mjs + desktop.mjs + mobile.mjs），走真实交互路径（对话选项点击/键盘/触屏/进店）而非直接调方法，成功抓到 3 个仅真流程才会暴露的 bug。

| 提交 | 内容 |
|---|---|
| `eb9fcf9` | **电脑端 33 项全过**：真实对话全流程（接→再谈推进→交付→结算）、键盘移动、视角切换、进店+掌柜对话+出店、Esc 暂停开/关、灯谜昼夜门控与答对答错、听书点唱、撑船竞速、真实购买茶叶、场景物指引、5 秒稳定运行、NaN 扫描。**手机端 14 项全过**：`?touch=1` 摇杆触控驱动移动、视角拖动、交谈按钮显隐与对话、菜单按钮暂停、视角按钮、竖屏 FOV=78。**修复**：①对话选项 `game.hud.update()` 缺参 → HUD.update(undefined) 崩溃并残留打开对话（冻结玩家、阻断后续交互）；②`chime()` 五声琶音索引越界 `[0,1,2,4,5]` → `PENTA2[5]=undefined` → NaN 频率，任何设备完成任务的庆祝音效都会崩溃，改 `[0,1,2,3,4]`；③暂停后 Esc 无法退出（暂停分支在输入处理前 return）；④全音频方法统一 `_ok()` 无音频设备防御；⑤竞速舟与毛脚夫重叠致交互被抢占，挪位至 (16,17) |

## 阶段二十：环境音"雪花噪声"修复

| 提交 | 内容 |
|---|---|
| `61ac1bf` | **问题**：背景能听到悦耳鸟叫，但同时有"雪花似的嘶声"。**根因**：环境音两层底噪（汴河水声、市井人声）用**原始白噪声**（`Math.random()`，全频平直）+ 单段低通，市井层还是 1.7kHz 带通——正是嘶声频段。**修复**：①改用**粉噪**（Paul Kellet，-3dB/oct，实测高频能量仅为白噪的 2%）；②汴河水声改粉噪→双段低通(420→760)陡降，无残留嘶声；③市井人声改粉噪→低通(900)→带通(1200,Q2)→增益 0.028→0.02，成远处人声嗡嗡而非嘶声；④雨声层同步换粉噪+限带(500-3800)。鸟鸣为正弦，保持悦耳不受影响。selftest/qa 全过 |

## 阶段二十一：游戏性第一梯队（主线闭环 + 经济/声望 + 世界痕迹）

> 基于"任务/游戏设计趣味性分析"，落地三项最高性价比优化。

| 提交 | 内容 |
|---|---|
| `3249611` | **主线「进京赶考」**：`locked` 前置（完成 3 个任务后由说书人解锁）+ 6 步剧情（说书人指点→取书稿→领路引→守将盖印→买盘缠→码头登船）→ 完成弹「金榜题名」**结局面板**（叙事 + 全程统计 + 分享/再游），给玩家一个"完成的意义"。**经济系统**：百杂铺钱掌柜开张杂货摊（对话购买）——画卷碎片 30 文（集 5 张拼成「珍藏画卷」成就卡）/ 花灯 20 文（逐盏点亮集市，世界变化）/ 香火 15 文（+10 声望）；买茶对话分支泛化为任意 `buy` 目标。**声望分级**：白衣书生→小有名声→汴京熟客→满城传扬四级，HUD 顶部称号芯片 + 升级 toast + 说书人高声望彩蛋对话。**世界痕迹感**：完成特定任务后世界可见变化——客栈添柴→烟囱冒烟、米铺送粮→多两袋米、醉仙楼送酒→码头多酒坛、码头送布→布庄多布卷，集市花灯随购买点亮。selftest 扩至 14 任务+经济+声望+世界全过，E2E 桌面 33/手机 14 全过 |

## 阶段二十二：世界布局重构（建筑两岸 · 无城门 · 远山）

| 提交 | 内容 |
|---|---|
| `448383e` | **新布局**：建筑由"一条南北大街"改为**分列拱桥两岸**——北岸 5 家（回春堂/醉仙楼/清风茶肆/悦来客栈/百杂铺，z 3~18）+ 南岸 5 家（锦绣布庄/张记肉铺/桂香点心/丰源米铺/宝香斋，z 46~62），共 10 家，河流保留、两岸由虹桥相连。**去城门**：移除城门楼/城墙/城门碰撞/小地图城门绘制，城门打卡改为「观山亭」；衙役/守将迁至桥北头巡检。**远山**：`buildMountains` 南北两列黛青山脊（近层 8 座 + 远雾层 4 座），入画出景。码头东移至北岸（x21~29），船/药草/布匹/竞速随迁。任务 NPC（货郎/茶博士/赵大夫/沈掌柜/船老大等）、可交互物、树木全部重排并避开建筑与码头。selftest/features/qa 全过，E2E 桌面 33/手机 14 全过，og-cover 与宣传海报已按新布局重出 |

## 经验教训（后续开发必读）

1. **真实 GPU 与 swiftshader 渲染有差异**：部分伪影仅真机出现，务必用"用户截图分析 + 真机调试工具（射线检测）"定位，勿只依赖本地无头测试。
2. **方向/坐标约定要一次定死并写注释**：右向量应为 `F×U`，曾因推导错误两度返工。
3. **后处理（composer）的 canvas 读回在 swiftshader 不可靠**：会产生假"灰蓝/反光"，需用 Chrome 原生截图或探针交叉验证。
4. **第一人称看不到自己的角色** 是行业默认，否则身体会挡视线（本次最大"幽灵"即此）。
5. **透明度正确实现**：ShaderMaterial 需手动处理 alpha，否则透明通道深度排序会闪烁/消失。
